# 📚 Быстрая справка по ChatBot

## 🎯 Как это работает (простыми словами)

1. **Пользователь печатает** → текст сохраняется в `inputValue`
2. **Нажимает "Отправить"** → вызывается `handleSendMessage()`
3. **Сообщение добавляется** → в массив `messages`
4. **Показывается "печатает..."** → `isTyping = true`
5. **Отправка на сервер** → через `sendChatMessage()`
6. **Получение ответа** → добавляется в `messages`
7. **Скрывается "печатает..."** → `isTyping = false`

---

## 📦 Структура файлов

```
ChatBot/
├── ChatBot.comp.jsx       # Главный компонент (UI + логика)
├── ChatBot.comp.css       # Стили
├── chatService.js         # API функции
├── README.md              # Документация
├── FLOW_DIAGRAM.md        # Визуальная диаграмма
├── INTEGRATION_EXAMPLE.md # Примеры интеграции
└── QUICK_REFERENCE.md     # Эта шпаргалка
```

---

## 🔑 Ключевые переменные State

| Переменная | Тип | Описание |
|------------|-----|----------|
| `isOpen` | boolean | Открыт ли чат |
| `messages` | array | История сообщений |
| `inputValue` | string | Текст в поле ввода |
| `isTyping` | boolean | Печатает ли бот |

---

## 🛠️ Основные функции

### `toggleChat()`
Открывает/закрывает окно чата
```javascript
const toggleChat = () => {
  setIsOpen(!isOpen);
};
```

### `handleSendMessage(e)`
Обрабатывает отправку сообщения
```javascript
const handleSendMessage = async (e) => {
  e.preventDefault();
  // 1. Создать сообщение пользователя
  // 2. Добавить в историю
  // 3. Отправить на сервер
  // 4. Получить ответ
  // 5. Добавить ответ в историю
};
```

### `scrollToBottom()`
Прокручивает к последнему сообщению
```javascript
const scrollToBottom = () => {
  messagesEndRef.current?.scrollIntoView({ behavior: "smooth" });
};
```

---

## 🌐 API функции (chatService.js)

### `sendChatMessage(message, sessionId)`
Отправляет сообщение на реальный backend

**Параметры:**
- `message` (string) - текст сообщения
- `sessionId` (string, optional) - ID сессии

**Возвращает:**
```javascript
{
  success: true/false,
  message: "Ответ от бота",
  sessionId: "123",
  timestamp: "2024-11-02T..."
}
```

### `sendChatMessageMock(message)`
Имитирует ответ для тестирования БЕЗ backend

**Параметры:**
- `message` (string) - текст сообщения

**Возвращает:** Тот же формат, что и `sendChatMessage`

---

## 🔄 Формат данных

### Сообщение в компоненте
```javascript
{
  text: "Привет!",
  sender: "user", // или "bot"
  timestamp: new Date()
}
```

### Запрос к API
```javascript
{
  message: "Привет!",
  sessionId: "session-123",
  timestamp: "2024-11-02T12:00:00.000Z",
  userId: "user-456"
}
```

### Ответ от API
```javascript
{
  success: true,
  message: "Здравствуйте!",
  sessionId: "session-123",
  timestamp: "2024-11-02T12:00:01.000Z"
}
```

---

## ⚙️ Как переключиться на реальный API

### Шаг 1: Обновите URL в `chatService.js`
```javascript
const API_CONFIG = {
  baseURL: 'https://your-api.com', // ← Ваш URL
  endpoints: {
    sendMessage: '/api/chat/message'
  }
};
```

### Шаг 2: В `ChatBot.comp.jsx` замените строку 48
```javascript
// Было (MOCK):
const response = await sendChatMessageMock(userMessage.text);

// Станет (реальный API):
const response = await sendChatMessage(userMessage.text);
```

---

## 🎨 Кастомизация

### Изменить цвета
В `ChatBot.comp.css`:
```css
/* Основной цвет кнопки */
.chat-toggle-btn {
  background: linear-gradient(135deg, #646cff 0%, #4a4fcf 100%);
}

/* Цвет сообщений пользователя */
.user-message .message-content {
  background: linear-gradient(135deg, #646cff 0%, #4a4fcf 100%);
}
```

### Изменить позицию
```css
.chatbot-container {
  bottom: 20px;  /* ← Расстояние снизу */
  right: 20px;   /* ← Расстояние справа */
}
```

### Изменить размер окна
```css
.chat-window {
  width: 380px;   /* ← Ширина */
  height: 550px;  /* ← Высота */
}
```

---

## 🐛 Отладка

### Проверить отправку сообщений
Откройте консоль браузера (F12) и отправьте сообщение. Вы увидите:
```
📤 Отправка запроса: {message: "Привет!", ...}
📥 Получен ответ: {success: true, message: "..."}
```

### Проверить состояние компонента
Добавьте в `ChatBot.comp.jsx`:
```javascript
console.log('Messages:', messages);
console.log('Is typing:', isTyping);
console.log('Is open:', isOpen);
```

### Проверить API запрос
В `chatService.js` уже есть логи:
```javascript
console.log('📤 Отправка запроса:', requestBody);
console.log('📥 Получен ответ:', data);
```

---

## ❓ Частые вопросы

### Q: Как добавить историю сообщений?
A: Используйте `localStorage`:
```javascript
// Сохранение
localStorage.setItem('chatHistory', JSON.stringify(messages));

// Загрузка при монтировании
useEffect(() => {
  const saved = localStorage.getItem('chatHistory');
  if (saved) setMessages(JSON.parse(saved));
}, []);
```

### Q: Как добавить файлы/изображения?
A: Добавьте input type="file" и используйте FormData:
```javascript
const formData = new FormData();
formData.append('message', message);
formData.append('file', file);
```

### Q: Как добавить typing indicator на стороне пользователя?
A: Отслеживайте событие `onChange` и отправляйте статус:
```javascript
const handleInputChange = (e) => {
  setInputValue(e.target.value);
  socket.emit('user-typing', { isTyping: true });
};
```

### Q: Как сделать чат только на определенных страницах?
A: В `App.jsx` условно рендерите:
```javascript
{location.pathname === '/' && <ChatBot />}
```

---

## 🚀 Быстрый тест

1. Откройте сайт
2. Нажмите на синюю кнопку в правом нижнем углу
3. Введите "Привет"
4. Нажмите отправить
5. Через 1 секунду увидите ответ

Если работает - всё настроено правильно! ✅

---

## 📞 Что дальше?

1. ✅ Протестируйте с MOCK данными
2. 🔧 Настройте свой backend
3. 🔄 Переключитесь на реальный API
4. 🎨 Кастомизируйте дизайн
5. 🚀 Деплой на продакшен
